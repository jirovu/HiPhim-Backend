#Spring config
spring:
  profiles:
    active: development
  #Token setup
  token:
    issuer: "HiPhim"
    secret: "my mama don't like you and she likes everyone"
    expirationTime: 86400000
  banner:
    image:
      location: banner.txt
  #Mail config
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: hiphimd16attt@gmail.com
    password: hiphimd16
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false

server:
  servlet:
    #Set cookie age
    session:
      cookie:
        max-age: 86400
        name: "JWT-TOKEN"

#Development Environment config
---
server:
  port: 1010

spring:
  profiles: development
  #MongoDB configuration
  data:
    mongodb:
      uri: mongodb://hiphim:hiphimd16@ds021326.mlab.com:21326/hiphim
      database: hiphim
  origin:
    url: "http://localhost:4200"
  #SqlServer configuration
  datasource:
    username: hiphim
    password: hiphim
    url: jdbc:mysql://localhost:3306/HiPhim?useUnicode=yes&characterEncoding=UTF-8
    sql-script-encoding: UTF-8



#Production Environment config
---
server:
  port: 2020

spring:
  profiles: production
  #MongoDB configuration
  data:
    mongodb:
      uri:
      database:
  origin:
    url: